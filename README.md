# sg-ask-me

Chatbot UI.

- **Backend**: See [backend/README.md](backend/README.md) for virtual environment setup, install, and run instructions (recommended: use a venv to avoid conflicts with global Python).
