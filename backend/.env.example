# Copy this file to .env and fill in values. Do not commit .env.
# For Docker: use env_file: .env in docker-compose or pass variables via environment: / secrets.

# ----------
# Required for default OpenAI provider
# ----------
OPENAI_API_KEY=sk-your-openai-key-here

# ----------
# Infrastructure (defaults shown; override for production)
# ----------
REDIS_URL=redis://localhost:6379/0
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/chatdb

# ----------
# API & CORS
# ----------
CORS_ORIGINS=http://localhost:3000
API_KEY=
# If set, requests must send X-API-Key header with this value.

# ----------
# Environment & LLM
# ----------
APP_ENV=local
LOG_LEVEL=INFO
# Which LLM provider to use (e.g. openai). Must match a key registered in app/services/llm/factory.py.
LLM_PROVIDER=openai
